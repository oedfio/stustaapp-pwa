ENVIRONMENT=local
DATABASE_URL=postgresql+asyncpg://stustaapp:devpassword@localhost:5432/stustaapp
REDIS_URL=redis://localhost:6379/0
JWT_SECRET_KEY=dev-secret-change-me
LOG_PATH=../logs/app.log
MEDIA_ROOT=../media
# Generate your own local keypair — see project_guide.md §6 for how.
# A placeholder here will crash push notifications, but everything else works.
VAPID_PRIVATE_KEY=dummy-for-local-dev
VAPID_PUBLIC_KEY=dummy-for-local-dev
VAPID_CLAIM_EMAIL=mailto:dev@localhost
